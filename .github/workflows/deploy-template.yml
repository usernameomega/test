name: 'Template For Docker Build, ECR Push And Helm Deploy Actions'

on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      app:
        type: string
        required: true

jobs:

  call-build-and-push:
    uses: ./.github/workflows/bp.yml
    with:
      environment: '${{ inputs.environment }}'
      env_vars: |
        MY_NEXT_VAR_1: ${{ vars.ENV_VAR }}
#    secrets:
#      env_vars: |
#        MY_NEXT_VAR: '${{ vars.ENV_VAR }}'
