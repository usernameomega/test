name: DEV Build/Push/Deploy

on:
  push:
    branches:
      - main
jobs:

  frontend:
    uses: ./.github/workflows/deploy-template.yml
    with:
      environment: 'development'
      app: 'frontend'
      ENV_VAR: ${{ vars.DEV_ENV_VAR }}
    secrets:
      ENV_SECRET: ${{ secrets.ENV_SECRET }}
